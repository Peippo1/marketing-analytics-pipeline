# Dependencies installed inside the Airflow image.
# We reuse the top-level pins to keep security fixes consistent across environments.
-r ../requirements.txt

# Airflow images already ship with the core scheduler/webserver bits; these are
# the extra libraries our DAGs and hooks rely on.
pyspark==3.5.4
delta-spark==3.2.0
pymysql==1.1.1
