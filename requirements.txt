# Security-updated dependency bundle used by CI and local development.
# Components are split to keep installs small and auditable.
-r requirements-fastapi.txt
-r requirements-streamlit.txt

mlflow==3.7.0
pyarrow==22.0.0
SQLAlchemy==2.0.44
python-dotenv==1.0.1
pyspark==3.5.4
delta-spark==3.2.0
